name: Deploy Dev

on:
  push:
    branches:
      - dev

jobs:
  terraform:
    runs-on: ubuntu-latest
    environment: dev

    concurrency:
      group: terraform-dev
      cancel-in-progress: true

    env:
      # Azure Service Principal para login e Terraform
      ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      ARM_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
      ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # Variáveis Terraform
      TF_VAR_secreto_do_cliente: ${{ secrets.AZURE_CLIENT_SECRET }}
      TF_VAR_id_do_cliente: ${{ secrets.AZURE_CLIENT_ID }}
      TF_VAR_id_do_tenant: ${{ secrets.AZURE_TENANT_ID }}
      TF_VAR_id_da_subscricao: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

    steps:
      # --------------------------
      # Checkout do código
      # --------------------------
      - uses: actions/checkout@v3

      # --------------------------
      # Setup Terraform
      # --------------------------
      - uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.7.0

      # --------------------------
      # Terraform Init
      # --------------------------
      - name: Terraform Init
        working-directory: infra
        run: terraform init -reconfigure

      # --------------------------
      # Terraform Plan
      # --------------------------
      - name: Terraform Plan
        working-directory: infra
        run: terraform plan -var-file=dev.tfvars -lock-timeout=300s

      # --------------------------
      # Terraform Apply
      # --------------------------
      - name: Terraform Apply
        working-directory: infra
        run: terraform apply -auto-approve -var-file=dev.tfvars -lock-timeout=300s

      # --------------------------
      # Pegar outputs do Terraform
      # --------------------------
      - name: Terraform Output
        id: tf-output
        working-directory: infra
        run: |
          echo "RESOURCE_GROUP=$(terraform output -raw nome_resource_group)" >> $GITHUB_ENV
          echo "FUNCTION_APP_NAME=$(terraform output -raw nome_function_app)" >> $GITHUB_ENV

      # --------------------------
      # Azure CLI login via SP
      # --------------------------
      - name: Azure CLI Login
        uses: azure/login@v1
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
              "subscriptionId": "${{ secrets.AZURE_SUBSCRIPTION_ID }}",
              "tenantId": "${{ secrets.AZURE_TENANT_ID }}"
            }

      # --------------------------
      # Zip do Azure Function
      # --------------------------
      - name: Package Azure Function
        run: |
          cd azure_function
          zip -r ../azure_function.zip ./*

      # --------------------------
      # Deploy da Azure Function
      # --------------------------
      - name: Deploy Azure Function
        run: |
          az functionapp deployment source config-zip \
            --name $FUNCTION_APP_NAME \
            --resource-group $RESOURCE_GROUP \
            --src azure_function.zip
